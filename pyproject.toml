[tool.pytest.ini_options]
# Agrega la raíz del repo a sys.path para que los tests importen el paquete
# `app` sin importar cómo se invoque pytest (console script o `python -m`).
pythonpath = ["."]
testpaths = ["tests"]
